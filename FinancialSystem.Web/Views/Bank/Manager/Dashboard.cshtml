@{
    ViewData["Title"] = "Manager Dashboard";
}

<div class="container mt-4">
    <h2 class="mb-4">Manager Dashboard</h2>

    <div class="row">
        <!-- Просмотр заявок на кредит/рассрочку -->
        <div class="col-md-6">
            <div class="card border-warning">
                <div class="card-body">
                    <h5 class="card-title">📋 Loan & Installment Requests</h5>
                    <p class="card-text">
                        Review and process loan and installment plan requests.
                    </p>
                    <a href="@Url.Action("PrepareLoanInstallmentRequests", "Bank", new { bankId = ViewBag.BankId })" class="btn btn-warning">
                        View Requests
                    </a>
                </div>
            </div>
        </div>
        
        <!-- Статистика по движениям средств + отмена перевода -->
        <div class="col-md-6">
            <div class="card border-primary">
                <div class="card-body">
                    <h5 class="card-title">📊 Transaction Statistics</h5>
                    <p class="card-text">
                        View statistics on user transactions and cancel recent transfers if needed.
                    </p>
                    <a href="@Url.Action("TransferStatistics", "Bank", new { bankId = ViewBag.BankId })" class="btn btn-primary">
                        View Statistics & Cancel Transfers
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <!-- Подтверждение заявки на зарплатный проект -->
        <div class="col-md-6">
            <div class="card border-success">
                <div class="card-body">
                    <h5 class="card-title">📑 Approve Salary Project</h5>
                    <p class="card-text">
                        Approve salary project requests after receiving enterprise data.
                    </p>
                    <a href="@Url.Action("PrepareSalaryProjectRequests", "Bank", new { bankId = ViewBag.BankId })" class="btn btn-success">
                        Approve Salary Project
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>